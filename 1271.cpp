#include <iostream>
#include <algorithm>
#include <string>
#include <cmath>
using namespace std;
int main(){
    int sum=0;
    int i=0;
    string a,b;
    cin>>a>>b;
    int a_len = a.length();
    int b_len = b.length();
    while(){
        int temp = pow(10,a_len - 1);
        sum+=temp/


    }
    return  0;
}